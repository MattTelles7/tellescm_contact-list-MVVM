<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:tellescm_contact_list_MVVM.Models"
             xmlns:viewModels="clr-namespace:tellescm_contact_list_MVVM.ViewModels"
             x:Class="tellescm_contact_list_MVVM.Views.ContactsPage"
             x:DataType="viewModels:ContactsViewModel"
             Title="Contacts">

    <Grid Padding="24" RowDefinitions="Auto,*" RowSpacing="16">
        <Button Grid.Row="0"
                Text="Add Another Contact"
                Command="{Binding AddContactCommand}" />

        <CollectionView x:Name="ContactList"
                        Grid.Row="1"
                        ItemsSource="{Binding Contacts}"
                        SelectionMode="Single"
                        SelectedItem="{Binding SelectedContact, Mode=TwoWay}"
                        SelectionChangedCommand="{Binding SelectContactCommand}">
            <CollectionView.EmptyView>
                <Label Text="No contacts have been added yet."
                       HorizontalOptions="Center"
                       VerticalOptions="Center"
                       TextColor="{StaticResource Gray500}" />
            </CollectionView.EmptyView>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:Contact">
                    <Grid Padding="0,14" RowDefinitions="Auto,Auto">
                        <Label Text="{Binding Name}"
                               FontAttributes="Bold"
                               FontSize="18" />
                        <Label Grid.Row="1"
                               Text="{Binding Email}"
                               TextColor="{StaticResource Gray500}" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>
